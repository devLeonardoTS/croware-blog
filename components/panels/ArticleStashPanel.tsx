type ArticleStashPanelProps = {};

const ArticleStashPanel = (props: ArticleStashPanelProps) => {
	return (
		<div className="p-4">
			<p>{"Artigos armazenados (não publicados)."}</p>
		</div>
	);
};

export default ArticleStashPanel;
